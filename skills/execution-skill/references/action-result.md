# Action Result

```json
{
  "action": "Add parser guard for missing webhook signature header.",
  "operation": "fs:write",
  "changedArtifacts": ["src/webhooks/signature.ts"],
  "commandsRun": [],
  "newInformation": ["The route accepts raw body bytes."],
  "outcome": "done"
}
```
